<UserControl x:Class="master.Views.ContractListBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:master.Views"
             xmlns:vms="clr-namespace:master.ViewModels"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="MyBorder" TargetType="Border">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="DarkGray"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="Function" TargetType="{x:Type ContentControl}">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Expander Header="{Binding Name}">
                            <TextBlock Text="{Binding Docs}" Margin="25,5,5,5"/>
                        </Expander>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Docs}" Value="">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Margin="25,3,0,3"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <ListBox dd:DragDrop.IsDragSource="True"
             dd:DragDrop.IsDropTarget="True"
             dd:DragDrop.UseDefaultEffectDataTemplate="True"
             HorizontalContentAlignment="Stretch"
             x:Name="listBox">
        <ListBox.Resources>

            <!--<DataTemplate DataType="{x:Type vms:VMfunction}">
                <Border Background="LightGray" BorderBrush="DarkGray" CornerRadius="4" BorderThickness="2">
                    <ContentControl Content="{Binding}" Style="{StaticResource TypedValueHelper}"/>
                </Border>
            </DataTemplate>-->

            <DataTemplate DataType="{x:Type vms:Y}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}">

                    </Expander>
                </Border>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vms:VMfunction}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}">
                        <TextBlock Text="{Binding Docs}" Margin="25,5,5,5"/>
                    </Expander>
                </Border>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vms:VMinput}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}" IsExpanded="True">
                        <StackPanel>
                            <ItemsControl HorizontalContentAlignment="Stretch"
                                          ItemsSource="{Binding Vars}">
                                <ItemsControl.Resources>
                                    <DataTemplate DataType="{x:Type vms:VMinputVariable}">
                                        <StackPanel Orientation="Horizontal" Margin="5,5,5,5">
                                            <Button Command="{Binding CommandRemove}" CommandParameter="{Binding}"  Margin="0,0,5,0">
                                                <Image Source="..\Images\cross_red.png" Width="16" Height="16"/>
                                            </Button>
                                            <TextBlock Margin="0,0,10,0" Text="Type:"/>
                                            <ComboBox Margin="0,0,10,0" Width="100" ItemsSource="{Binding Types}" SelectedIndex="0"/>
                                            <TextBlock Margin="0,0,10,0" Text="Object:"/>
                                            <ComboBox Margin="0,0,10,0" Width="100" ItemsSource="{Binding ObjectNames}" SelectedIndex="0"/>
                                            <TextBlock Margin="0,0,10,0" Text="Alias:"/>
                                            <TextBox Margin="0,0,10,0" Width="100" Text="{Binding Alias}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.Resources>
                            </ItemsControl>
                            <Button Command="{Binding CommandAdd}" Margin="0,0,5,0">
                                <Image Source="..\Images\add_green.png" Width="16" Height="16"/>
                            </Button>
                        </StackPanel>
                    </Expander>
                </Border>
            </DataTemplate>
        </ListBox.Resources>
    </ListBox>
</UserControl>
