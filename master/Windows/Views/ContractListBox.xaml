<UserControl x:Class="master.Windows.Views.ContractListBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:master.Windows.Views"
             xmlns:vm_contract="clr-namespace:master.ViewModels.Contract"
             xmlns:vm_blocks="clr-namespace:master.ViewModels.Contract.Block.Blocks"
             xmlns:vm_block="clr-namespace:master.ViewModels.Contract.Block"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style x:Key="MyBorder" TargetType="Border">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="DarkGray"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="Function" TargetType="{x:Type ContentControl}">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Expander Header="{Binding Name}">
                            <TextBlock Text="{Binding Docs}" Margin="25,5,5,5"/>
                        </Expander>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Docs}" Value="">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Margin="25,3,0,3"/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <ListBox dd:DragDrop.IsDragSource="True"
             dd:DragDrop.IsDropTarget="True"
             dd:DragDrop.UseDefaultEffectDataTemplate="True"
             dd:DragDrop.DropHandler="{Binding}"
             HorizontalContentAlignment="Stretch"
             x:Name="listBox">
        <ListBox.Style>
            <Style TargetType="ListBox">
                <Setter Property="ItemsSource" Value="{Binding Blocks}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsContract}" Value="True">
                        <Setter Property="ItemsSource" Value="{Binding Functions}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsFunction}" Value="True">
                        <Setter Property="ItemsSource" Value="{Binding Blocks}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ListBox.Style>
        <ListBox.Resources>

            <DataTemplate DataType="{x:Type vm_contract:VMfunction}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}">
                        <TextBlock Text="{Binding Docs}" Margin="25,5,5,5"/>
                    </Expander>
                </Border>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm_blocks:VMassign}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}" IsExpanded="True">
                        <StackPanel Orientation="Horizontal" Margin="5,5,5,5">
                            <TextBlock Margin="0,0,10,0" Text="Alias:"/>
                            <ComboBox Margin="0,0,10,0" Width="100">
                                <ComboBoxItem>First</ComboBoxItem>
                                <ComboBoxItem>Second</ComboBoxItem>
                                <ComboBoxItem>Third</ComboBoxItem>
                            </ComboBox>
                            <TextBlock Margin="0,0,10,0" Text="Action:"/>
                            <ComboBox Margin="0,0,10,0" Width="100">
                                <ComboBoxItem>Insert</ComboBoxItem>
                                <ComboBoxItem>Update</ComboBoxItem>
                                <ComboBoxItem>Remove</ComboBoxItem>
                            </ComboBox>
                            <CheckBox Margin="0,0,10,0" Content="Direct"/>
                        </StackPanel>
                    </Expander>
                </Border>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm_blocks:VMlog}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}" IsExpanded="True">
                        <TextBox AcceptsReturn="True" Margin="5,5,5,5" Text="{Binding Text}">
                            
                        </TextBox>
                    </Expander>
                </Border>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm_blocks:VMinput}">
                <Border Style="{StaticResource MyBorder}">
                    <Expander Header="{Binding Name}" IsExpanded="True">
                        <StackPanel>
                            <ItemsControl HorizontalContentAlignment="Stretch"
                                          ItemsSource="{Binding Vars}">
                                <ItemsControl.Resources>
                                    <DataTemplate DataType="{x:Type vm_block:VMvariable}">
                                        <StackPanel Orientation="Horizontal" Margin="5,5,5,5">
                                            <Button Command="{Binding CommandRemove}" CommandParameter="{Binding}"  Margin="0,0,5,0">
                                                <Image Source="..\Images\cross_red.png" Width="16" Height="16"/>
                                            </Button>
                                            <TextBlock Margin="0,0,10,0" Text="Type:"/>
                                            <ComboBox Margin="0,0,10,0" Width="100" ItemsSource="{Binding Types}" SelectedItem="{Binding Type}" IsEditable="False"/>
                                            <TextBlock Margin="0,0,10,0" Text="Object:"/>
                                            <ComboBox Margin="0,0,10,0" Width="100" ItemsSource="{Binding ObjectNames}" IsEnabled="{Binding IsObject}" IsEditable="False"/>
                                            <CheckBox Margin="0,0,10,0" IsChecked="{Binding List}" Content="List" FlowDirection="LeftToRight"/>
                                            <TextBlock Margin="0,0,10,0" Text="Alias:"/>
                                            <TextBox Margin="0,0,10,0" Width="100" Text="{Binding Alias}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.Resources>
                            </ItemsControl>
                            <Button Command="{Binding CommandAdd}" Margin="0,0,5,0">
                                <Image Source="..\Images\add_green.png" Width="16" Height="16"/>
                            </Button>
                        </StackPanel>
                    </Expander>
                </Border>
            </DataTemplate>
        </ListBox.Resources>
    </ListBox>
</UserControl>
